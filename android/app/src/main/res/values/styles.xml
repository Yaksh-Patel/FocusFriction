<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
  </style>
  <!-- PauseActivity extends android.app.Activity, so this parents an android:
       theme rather than an AppCompat one. Opaque, no action bar, no transition —
       the pause should feel like it was already there. -->
  <style name="Theme.App.Pause" parent="@android:style/Theme.DeviceDefault.NoActionBar">
    <item name="android:windowBackground">@color/ff_window</item>
    <item name="android:statusBarColor">@color/ff_window</item>
    <item name="android:navigationBarColor">@color/ff_window</item>
    <item name="android:windowAnimationStyle">@null</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/splashscreen_logo</item>
  </style>
</resources>